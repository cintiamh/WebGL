// Generated by CoffeeScript 1.4.0
(function() {
  var ASPECT, BLOCK_SIZE, FAR, HEIGHT, NEAR, SPLITX, SPLITY, SPLITZ, Tetris, VIEW_ANGLE, WIDTH, prepareCanvas, start;

  WIDTH = 600;

  HEIGHT = 600;

  SPLITX = 6;

  SPLITY = 6;

  SPLITZ = 20;

  BLOCK_SIZE = WIDTH / SPLITX;

  VIEW_ANGLE = 45;

  ASPECT = WIDTH / HEIGHT;

  NEAR = 0.1;

  FAR = 10000;

  Tetris = {};

  prepareCanvas = function() {
    var camera, cube, renderer, scene;
    canvas.width = WIDTH;
    canvas.height = HEIGHT;
    camera = new THREE.PerspectiveCamera(VIEW_ANGLE, WIDTH / HEIGHT, 1, 10000);
    camera.position.z = 300;
    scene = new THREE.Scene;
    cube = new THREE.Mesh(new THREE.CubeGeometry(50, 50, 50), new THREE.MeshBasicMaterial({
      color: 0xFF0000
    }));
    scene.add(cube);
    renderer = new THREE.CanvasRenderer(canvas);
    renderer.setSize(WIDTH, HEIGHT);
    renderer.setClearColor(0xEEEEEE, 1.0);
    return renderer.clear;
  };

  start = function() {
    return prepareCanvas();
  };

}).call(this);
